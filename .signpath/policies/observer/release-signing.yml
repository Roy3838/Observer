# SignPath GitHub Policy for Observer release-signing (production OV certificate)
# See: https://docs.signpath.io/trusted-build-systems/github

github-policies:
  runners:
    require_github_hosted: true
  build:
    disallow_reruns: true
